import { combineReducers } from 'redux';

import '../fonts/blacklisted/Blacklisted.ttf';
import '../fonts/ocr-a/ocr-aregular.ttf';

const rootReducer = combineReducers({
  state: (state = {}) => state
});

export default rootReducer;